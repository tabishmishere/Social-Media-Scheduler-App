services:
  server:
    image: social-scheduler-server:latest
    build: ./server
    env_file: ./server/.env

  client:
    image: social-scheduler-client:latest
    build:
      context: ./client
      args:
        VITE_API_BASE_URL: /
    depends_on:
      - server
    ports:
      - "5173:80"
